# These are build-time env vars (mapped via next.config.ts env field).
# They are embedded in the static export bundle — use a tenant-scoped API key.
API_KEY=your_api_key_here
API_BASE_URL=https://api.prxyauth.xyz/api